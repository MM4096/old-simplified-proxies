class Card {
	constructor(name="", manaCost="", type="", text="", power="", toughness="", flavorText="", notes="") {
		this.name = name;
		this.manaCost = manaCost;
		this.type = type;
		this.text = text;
		this.power = power;
		this.toughness = toughness;
		this.flavorText = flavorText;
		this.notes = notes;
	}
}

function getIcon(code) {

}

function setPreview(card) {

}